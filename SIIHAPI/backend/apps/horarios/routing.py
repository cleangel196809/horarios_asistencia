"""SIIHAPI · WebSocket routing para horarios (RF-34)."""
from django.urls import re_path
# from .consumers import HorarioConsumer

websocket_urlpatterns = [
    # re_path(r'ws/horarios/$', HorarioConsumer.as_asgi()),
]
