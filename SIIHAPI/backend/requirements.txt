# SIIHAPI Backend - Django 5 + DRF + Oracle XE 21c + Argon2id
# Politecnico Internacional - 2026
#
# Versiones flexibles (>=) para compatibilidad con Python 3.11, 3.12, 3.13 y 3.14

# Core Django
Django>=5.0,<5.3
djangorestframework>=3.15
django-cors-headers>=4.3
django-environ>=0.11

# Argon2id para hash de 128 caracteres (RNF-07)
argon2-cffi>=23.1
argon2-cffi-bindings>=21.2

# PostgreSQL (base unificada integracion_pi / Neon) — Fase 2, 2026-09-03
psycopg[binary]>=3.1

# Autenticacion JWT
djangorestframework-simplejwt>=5.3
PyJWT>=2.8

# Documentacion API
drf-spectacular>=0.27

# WebSockets
channels>=4.1
channels-redis>=4.2
daphne>=4.1

# Cliente HTTP
requests>=2.32
httpx>=0.27

# Cache y cola
redis>=5.0
django-redis>=5.4
celery>=5.4

# Reportes - exportacion multi-formato
reportlab>=4.2       # PDF
qrcode[pil]>=7.4     # QR deterministico para Eventos (apps.eventos)
openpyxl>=3.1        # Excel
python-docx>=1.1     # Word .docx
python-pptx>=0.6     # PowerPoint .pptx

# Utilidades
python-dotenv>=1.0
gunicorn>=22.0
whitenoise>=6.6

# Tests
pytest>=8.2
pytest-django>=4.8
pytest-cov>=5.0.0
pytest-mock>=3.14.0
faker>=25.2

# === MOTOR IA HIBRIDO ===
# CSP Solver profesional (RF-26 a RF-37)
ortools>=9.10           ; python_version < "3.14"

# LLM providers (instalar SOLO el que vayas a usar)
google-generativeai>=0.7  ; python_version < "3.14"
openai>=1.30
anthropic>=0.30

# Manipulacion tabular (gestion documental)
pandas>=2.2
